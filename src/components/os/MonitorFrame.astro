---
interface Props {
  label?: string;
}

const { label = 'Burak OS monitor' } = Astro.props as Props;
---

<section class="monitor-frame" aria-label={label}>
  <div class="monitor-bezel">
    <div class="monitor-screen">
      <slot />
    </div>
  </div>
</section>
